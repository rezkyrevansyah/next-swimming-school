/** Minutes after class start time before attendance is marked 'late' */
export const LATE_THRESHOLD_MINUTES = 15;

/** Seconds until a member QR token expires and must be refreshed */
export const QR_TOKEN_EXPIRY_SECONDS = 30;

/** Maximum file size allowed for uploads, in megabytes */
export const MAX_FILE_SIZE_MB = 5;

/** Minutes of inactivity before the session is considered expired */
export const SESSION_TIMEOUT_MINUTES = 60;

/** Minimum allowed password length for all users */
export const PASSWORD_MIN_LENGTH = 8;

/** App name used in page titles and metadata */
export const APP_NAME = "Next Swimming School";

/** Default pagination page size */
export const DEFAULT_PAGE_SIZE = 20;
